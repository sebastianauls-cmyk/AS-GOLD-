name: V46 Workspace Refactor (manual legacy)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout modular branch
        uses: actions/checkout@v4
        with:
          ref: v46-navigation-clean
          fetch-depth: 1
      - name: Legacy workflow intentionally manual
        run: |
          echo 'The automatic bulk refactor chain is frozen. Current V46 work proceeds through reviewed modular changes and the normal Vercel regression build.'
